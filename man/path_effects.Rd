% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path_effects.R
\name{path_effects}
\alias{path_effects}
\title{Analyse Multivariate Path Effects}
\usage{
path_effects(
  relations,
  blocks,
  validation,
  segments,
  SO = TRUE,
  fits = FALSE,
  boot = 0,
  ncomp = NULL,
  transitive_closure = FALSE,
  ...
)
}
\arguments{
\item{relations}{A matrix defining the path structure. Rows specify relationships
between blocks, with 2 columns: \code{c(from_block_index, to_block_index)}.}

\item{blocks}{A multiblock data frame or list of named matrices containing the block data.}

\item{validation}{Validation method passed to \code{\link[pls:mvr]{pls::plsr()}}. Common options:
\code{"CV"} for cross-validation, "LOO" for leave-one-out validation, or \code{"none"} for fitted values.}

\item{segments}{Number of segments for cross-validation. Default is 5.}

\item{SO}{Logical. If \code{TRUE} (default), use SO-PLS; if \code{FALSE}, use OLS.}

\item{fits}{Logical. If \code{TRUE}, use fitted values instead of cross-validation.
Default is \code{FALSE}.}

\item{boot}{Number of bootstrap samples for computing standard errors.
Default is 0 (no bootstrapping).}

\item{ncomp}{Optional component settings per predictor block.
Supply either:
\itemize{
\item a vector with length equal to the number of predictor blocks (\code{unique(relations[,1])}),
in ascending predictor-block order, or
\item a full-length vector with one entry per block (non-predictors may be \code{0}).
Positive integers set upper limits; negative integers force exact component counts.
All predictor entries must be non-zero and have the same sign.
}}

\item{transitive_closure}{Logical. If \code{TRUE}, automatically add transitive closure to the relations. Default is \code{FALSE}.}

\item{...}{Additional arguments passed to underlying fitting functions.}
}
\value{
An object of class \code{path_effects}, which is a matrix with the following
components for each path:
\itemize{
\item \code{Tot}: Total effect
\item \code{Un}: Unique effect
\item \code{Co}: Common contribution
\item \code{Ad}: Additional effect
}

Attributes include:
\itemize{
\item \code{scaled}: Scaled contribution matrix
\item \code{individual}: Individual-level contributions
\item \code{boot}: Bootstrap replicates (if \code{boot > 0})
}
}
\description{
Decomposes effects between blocks in a multiblock system into total effects, unique effects,
common contributions, and additional effects. Supports both SO-PLS (Sequential Orthogonalised PLS)
and ordinary least squares (OLS) approaches with cross-validation or fitted values.
}
\examples{
# Analysis of the mobile dataset
data(mobile)

# Define path structure (A->B, A->E, A->G, B->C, B->D, B->E, C->D, D->E, 
#                        D->E, E->F, E->G, F->G)
paths <- matrix(c(1,2, 1,5, 1,7, 2,3, 2,4, 2,5, 3,4, 3,5, # Add 0,2, for A->C
                  4,5, 5,6, 5,7, 6,7),
                ncol=2, byrow=TRUE)

# Compute path effects with cross-validation using SO-PLS
pem <- path_effects(paths, mobile, validation="CV", segments=5, 
                    segment.type="consecutive")

# Print results
print(pem)

# Plot all results
plot(pem)

# Print and plot single path
print(pem, "A","G")
plot(pem, from = "A", to = "G")

# Print and plot results per variable
print(pem, individual = TRUE)
plot(pem, individual = TRUE)

# Analysis of the NIR-Raman-PUFA data (emulsions) 
data(emulsions)

# Standardise response
emulsions$PUFA <- scale(emulsions$PUFA)

# Define path structure (NIR->Raman, NIR->PUFA, Raman->PUFA)
paths_NRP <- matrix(c(1,2,1,3,2,3), ncol = 2, byrow = TRUE)

\dontrun{ # Too time consuming
# Compute path effects with cross-validation using SO-PLS
pem_NRP <- path_effects(paths_NRP, emulsions, validation="CV", 
                    segments = 5, segment.type="consecutive", 
                    ncomp=c(16,15))
                    
# Print results
print(pem_NRP)

# Plot all results
plot(pem_NRP)
}
# Reversed order of NIR and Raman (uncomment to run)
# paths_RNP <- matrix(c(2,1,2,3,1,3), ncol = 2, byrow = TRUE)
# pem_RNP <- path_effects(paths_RNP, emulsions, validation="CV",
#                   segments = 5, segment.type="consecutive", ncomp=c(16,15))
# print(pem_RNP)
}
\seealso{
\code{\link[=print.path_effects]{print.path_effects()}}, \code{\link[=plot.path_effects]{plot.path_effects()}}
}
